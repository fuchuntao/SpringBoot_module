server:
  port: 8861
spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared   # 默认是在 resource 的 config 目录下